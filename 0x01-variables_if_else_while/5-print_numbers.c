#include <stdio.h>
/**
 * main - single digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s;

	for (s = 0; s < 10; s++)
		printf("%d", s);
	printf("\n");
	return (0);
}
