#include "main.h"
/**
 * add - adds two integers and returns result
 * @a: one of integers added
 * @b: added integer
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	int p;

	p = a + b;

	return (p);
}
