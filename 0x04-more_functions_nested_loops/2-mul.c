#include "main.h"
/**
 * mul - multiplies two integers
 * @a: multiplied integer
 * @b: another multiplied integer
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int p;

	p = a * b;

	return (p);
}
